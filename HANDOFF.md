# HANDOFF: перенос проекта в Cowork

## Что это за проект

Трекер полезных привычек. Пет-проект одного человека, цель — научиться собирать приложение с нуля через вайб-кодинг. Автор — системный аналитик, не разработчик.

## Где мы сейчас

**Стадия:** план MVP пройден целиком — шаги 1-5b сделаны, все пятнадцать находок ручного тестирования разобраны и закрыты (см. «Статус находок»). Сверх плана сделан шаг 6 — синхронизация чата с действиями в вебе.

Готово:
- `docs/requirements.md` — полная спека MVP v1.3, актуализирована по итогам находок
- `CLAUDE.md` — правила работы с кодовой базой и инварианты
- **Шаг 1:** compose на шесть сервисов, образы бэка и фронта, конфиг, четыре модели, миграция `0001`, три точки входа
- **Шаг 2:** `app/services/` — восемь модулей, миграция `0002`, фикстуры
- **Шаг 3:** `app/scheduler/` — три джоба и расписание APScheduler, `services/messages.py`, `LogNotifier`
- **Шаг 4:** `app/telegram.py` — `TelegramNotifier`, `app/bot/handlers.py` — хендлеры кнопок и `/start`, `services/users.py`
- **Шаг 5a:** `app/api/` — дев-логин, роутеры привычек/occurrences/пользователя, схемы, единый обработчик доменных ошибок
- **Шаг 5b:** `frontend/src/` — пять экранов раздела 9, клиент API, мини-роутер на хэше, прокси Vite вместо CORS
- **Шаг 6:** `sent_messages` и миграция `0003`, четвёртый джоб `close_chat_messages` — действие в вебе гасит сообщение в чате (находка 2)
- **Документация:** `README.md`, `docs/erd.md` (ER-диаграмма), `docs/architecture.md` (C4 и пять сиквенсов) — всё на PlantUML

Каждый шаг проверен и руками на живом стенде (curl/Playwright/psql/Telegram), и автотестами; подробности конкретных прогонов не дублируются здесь — они в истории git и в коммитах соответствующих сессий. Текущее состояние: `pytest` — 124 теста, зелёный; `tsc` и `npm run build` без ошибок.

Все пятнадцать находок закрыты, что именно сделано — в разделе «Статус находок».

### Состав сервисного слоя

| Модуль | Что внутри |
|---|---|
| `timeutils` | единственное место перехода UTC ↔ локальное время |
| `constants` | числа из спеки: 5 минут, 5 снузов, 14 дней паузы, 30 дней окна |
| `errors` | доменные исключения, которые ловят адаптеры |
| `occurrences` | переходы статусов (§4) и выборки диспетчера (§6.2, §6.3) |
| `generation` | создание occurrences по расписанию (§6.1) |
| `pauses` | отрезки пауз с учётом досрочного снятия |
| `habits` | создание, правка, архив, пауза, смена таймзоны, пересечения (§8, §9) |
| `stats` | стрики и статистика (§7) |
| `messages` | тексты уведомлений, наборы кнопок и разбор `callback_data` (§5) |
| `sent_messages` | учёт отправленного в чат и выборка того, что пора погасить (§6.4) |
| `users` | поиск пользователя по `telegram_id` |

Переходы статусов синхронные и не трогают сессию: они меняют загруженный объект, а `commit` делает вызывающий адаптер. Поэтому одно действие пользователя остаётся одной транзакцией, а правила проверяются юнит-тестами без Postgres.

### Состав API

| Файл | Что внутри |
|---|---|
| `app/api/deps.py` | `SessionDep`, `CurrentUser` (дев-токен), `OwnedHabit` (привычка + проверка владения) |
| `app/api/schemas.py` | Pydantic-схемы запросов/ответов, без бизнес-логики |
| `app/api/routers/auth.py` | `POST /auth/dev-login` |
| `app/api/routers/users.py` | `GET/PATCH /users/me` — профиль и смена таймзоны, `GET /users/timezone-preview` — предпросмотр переезда |
| `app/api/routers/habits.py` | CRUD привычек, паузы, проверка пересечений, статистика, heatmap |
| `app/api/routers/occurrences.py` | список на день + четыре кнопки-перехода |
| `app/api/main.py` | сборка роутеров, единый обработчик `ServiceError` → HTTP-код, `IntegrityError` → 409 |

Роутер — такой же тонкий адаптер, как джоб и хендлер бота (инвариант 4): разобрал вход, позвал сервис, закоммитил, отдал ответ. Решений о статусах и бизнес-правилах в `app/api/` нет ни одного — они все уже были в `services/` и написаны на шаге 2.

### Состав планировщика

| Файл | Что внутри |
|---|---|
| `scheduler/jobs.py` | четыре джоба: `generate_occurrences`, `dispatch_notifications`, `close_chat_messages`, `close_days` |
| `scheduler/main.py` | `AsyncIOScheduler`, регистрация джобов, graceful shutdown |

Джоб — такой же тонкий адаптер, как роутер и хендлер бота (инвариант 4): открыл сессию, позвал сервис, позвал `Notifier`, записал итог в лог. Решений о статусах в `scheduler/` нет ни одного.

| Джоб | Расписание | Что зовёт |
|---|---|---|
| §6.1 генератор | `03:00 UTC` плюс разовый прогон при старте воркера | `generation.generate_all` |
| §6.2 диспетчер | каждые `SCHEDULER_TICK_SECONDS` | `occurrences.due_for_notification` / `due_for_followup`, затем `mark_notified` / `mark_followup_sent`; попутно гасит сообщения, вытесненные снузом |
| §6.4 закрытие сообщений | каждые `SCHEDULER_TICK_SECONDS` | `sent_messages.due_for_closing`, затем `Notifier.close` и `mark_closed` |
| §6.3 закрытие дня | ежечасно в `:00` | `occurrences.close_finished_days` |

### Состав бота

| Файл | Что внутри |
|---|---|
| `app/telegram.py` | `create_bot`, `TelegramNotifier`, сборка инлайн-клавиатуры |
| `app/bot/handlers.py` | `/start`, хендлер кнопок, разбор доменных ошибок в реплики |
| `app/bot/main.py` | `Dispatcher`, polling, закрытие сессий на выходе |

Хендлер — такой же тонкий адаптер, как джоб (инвариант 4): разобрал `callback_data`, загрузил запись через `occurrences.get_for_user`, позвал переход, сделал `commit`, ответил человеку. Решений о статусах в `app/bot/` нет ни одного.

| Кнопка | Что зовёт |
|---|---|
| ▶️ Начал | `occurrences.start` |
| ⏰ +5 мин | `occurrences.snooze` |
| ✅ Выполнил | `occurrences.complete` |
| 🚫 Пропустить сегодня / ❌ Не получилось | `occurrences.skip` |

Оговорки, которые всплыли на шагах 1-4:
- Версии фронта зафиксированы `package-lock.json`, образ ставит их через `npm ci`. Без lock-файла установка занимала ~25 минут вместо 7 секунд
- `paused` считается нетерминальным статусом: §4 называет его терминальным, но там же описан переход `paused → pending`
- `pytest` ставится в тот же образ через `.[dev]`. Проект локальный, отдельная сборка под тесты сложнее, чем польза от неё
- В образе стоит APScheduler 3.11. Это важно: 3.10 принимал только таймзоны pytz, а 3.11 перешёл на `zoneinfo`. В коде таймзона везде передаётся строкой `"UTC"` — её разбирают обе версии
- Логи `apscheduler.executors` приглушены до WARNING: на отладочном тике в 5 секунд две строки INFO на каждый запуск джоба забивают вывод
- В образе стоит aiogram 3.30. Синтаксис v2 несовместим полностью, а v4 уже вышел и тоже ломающий — проверяй, что подсказка не из другого поколения
- `TELEGRAM_BOT_TOKEN` пуст — всё поднимается: воркер уходит на `LogNotifier` и пишет об этом, бот логирует предупреждение и просто ждёт
- **Правку `.env` подхватывает `docker compose up -d <сервис>`, а не `restart`.** `restart` поднимает контейнер с тем окружением, с которым тот был создан, и молча работает по-старому. Правку кода, наоборот, хватает и `restart`: исходники смонтированы томом. Ловится по строчке «тик диспетчера = N c» в логе воркера

## Ключевые решения и почему они такие

Эти решения обсуждались и приняты осознанно. Не предлагай их изменить без веской причины.

| Решение | Обоснование |
|---|---|
| Уведомления через Telegram, не web push | В Safari/iOS нет кнопок в уведомлениях, а кнопки — суть продукта |
| Веб-интерфейс на React + Telegram как транспорт | Веб для CRUD и статистики, TG для уведомлений с кнопками |
| Habit и Occurrence — разные сущности | Без второй невозможны статусы, история и стрики |
| Occurrences генерируются заранее ночным джобом | Проще планировщик и отладка, видно что запланировано |
| Планировщик опрашивает БД, а не держит таймеры | Таймеры не переживают рестарт контейнера |
| Polling у бота, не webhook | Локально webhook требует туннеля |
| Длительность по умолчанию 5 минут, минимум 5 | Убирает класс «привычек без длительности» |
| Таймер выполнения от нажатия «Начал» | Человек может начать не ровно в плановое время |
| Снуз максимум 5 раз по 5 минут | Дальше — `missed` |
| `skipped` и `missed` ломают стрик одинаково | Решение автора: осознанный отказ не лучше игнора |
| Пауза не ломает стрик, но >14 дней обнуляет | Компромисс между мягкостью и эксплойтом «ставлю паузу когда лень» |
| Восстановление из архива обнуляет стрик | Иначе архив = бесконечная пауза в обход правила 14 дней |
| Монорепозиторий, не отдельные репо | Compose описывает оба; изменения контракта должны быть атомарными |
| Всё в Docker Compose, локально | Цель проекта — научиться, а не выкатить в прод |
| Ретроспективная отметка вне MVP | Обесценивает статистику |
| `notified → done` и `in_progress → skipped` разрешены (шаг 2) | §4 их не описывает, но §5 даёт эти кнопки на догоняющем пинге. Без них кнопка «Выполнил» тому, кто не отреагировал, не работала бы |
| «Не получилось» → `skipped`, не `missed` (шаг 2) | Человек ответил осознанно. `missed` по §11 означает «не отреагировал вовсе» и потому неизменяем |
| Колонка `Habit.streak_reset_on` (шаг 2, миграция `0002`) | §8 требует обнулить серию при восстановлении из архива, но прошлые `done` остаются в occurrences. Без отметки серия продолжилась бы. Рекорд считается по всей истории и отметку игнорирует |
| Снуз отсчитывает 5 минут от нажатия, а не от старого `current_due_at` (шаг 2) | По букве §4 это `+= 5`, но если отреагировать через 20 минут, новое время окажется в прошлом и напоминание придёт мгновенно. Кнопка обещает «через пять минут» |
| Догоняющий пинг для `notified` считается от `GREATEST(current_due_at, notified_at)` (шаг 3) | По букве §6.2 это `current_due_at + duration`. Но если occurrence просрочен дольше своей длительности (воркер стоял, сид сделан давно), оба условия §6.2 истинны в одном тике и человек получает «пора» и «успел?» подряд. В штатном режиме `notified_at ≈ current_due_at` и поведение то же. По смыслу это та же поправка, что и в снузе |
| Закрытие дня не проверяет «наступила ли полночь» (шаг 3) | Для каждого пользователя берётся его текущая локальная дата и закрывается всё, что раньше неё. Результат тот же, но не надо помнить, когда джоб отработал в прошлый раз: повторный запуск в том же часу находит 0 записей (инвариант 7). Заодно суточный простой контейнера закроет все пропущенные дни, а не только последний |
| Диспетчер сначала отправляет, потом ставит отметку (шаг 3) | Обратный порядок при сбое отправки означал бы, что человек уведомления не увидел, а occurrence уже `notified`. Падение между отправкой и коммитом даёт повтор на следующем тике — это заметно менее неприятно. Всё внутри одной транзакции, строки держит `FOR UPDATE ... SKIP LOCKED` |
| Разовый прогон генератора при старте воркера (шаг 3) | Расписание APScheduler живёт в памяти, поэтому пропущенное за простой контейнера 03:00 сам он не отработает. Джоб идемпотентен, лишний запуск ничего не портит |
| Тексты уведомлений размечены HTML, название экранируется (шаг 3) | Легаси-Markdown у Telegram спотыкается на подчёркиваниях и звёздочках в пользовательском тексте, в HTML достаточно экранировать три символа. Название привычки вводит пользователь |
| Набор кнопок первого уведомления не спрашивает `occurrences.can_snooze` (шаг 3) | `can_snooze` требует статуса `notified`, а диспетчер сначала отправляет и только потом помечает — на момент сборки текста запись ещё `pending`, и кнопка «+5 мин» не показывалась никогда. `reminder()` вызывается ровно для уходящего уведомления, поэтому проверяет только остаток снузов. Нашлось на живом прогоне: юнит-тест закреплял ошибочное поведение |
| Ошибка отправки не роняет весь тик (шаг 3) | Одна недоставленная запись не должна мешать остальной пачке. Отметка при этом не ставится — попробуем на следующем тике |
| `TelegramNotifier` отдельным модулем `app/telegram.py` (шаг 4) | В `notifier.py` остались протокол и `LogNotifier`, и он ничего не знает про aiogram. Этот модуль тянет `services/messages.py`, а через него — юнит-тесты; подтягивать за собой пол-фреймворка им незачем |
| `parse_mode=HTML` задан один раз в `create_bot` через `DefaultBotProperties` (шаг 4) | Тексты уже размечены HTML. Указывать разметку в каждом вызове означало бы однажды забыть и показать человеку сырые теги |
| Разбор `callback_data` лежит в `services/messages.py` рядом со сборкой (шаг 4) | Формат один. Если собирать строку в сервисах, а разбирать в боте, они однажды разойдутся — и молча, потому что кнопка просто перестанет отвечать |
| `parse_callback_data` возвращает `None`, а не поднимает исключение (шаг 4) | Строка приходит снаружи и может быть какой угодно: от кнопки старой версии бота до подделки. Это штатный ввод, а не сбой |
| После нажатия бот дописывает итог в сообщение и убирает кнопки (шаг 4) | Кнопка под старым сообщением живёт вечно. Без отметки через день не понять, отвечал ты на это уведомление или нет. Текст итога (`messages.action_note`) в сервисах, чтобы веб на шаге 5 говорил то же самое |
| Доменные ошибки — реплика через `callback.answer`, а не падение (шаг 4) | `AlreadyInStatus`, `InvalidTransition` и `SnoozeLimitReached` на кнопках — норма, а не баг: двойное нажатие и нажатие на уже закрытом занятии случаются постоянно |
| `/start` не заводит пользователей (шаг 4) | Их создаёт сид (§12.5), а `telegram_id` уникален — заведённый ботом пользователь подрался бы с сид-пользователем. `/start` показывает id, чтобы его было откуда взять для `.env` |
| `allowed_updates` ограничен `message` и `callback_query` (шаг 4) | Остальные типы апдейтов Telegram копит, пока контейнер стоит, и отдаёт пачкой при старте |
| Есть хендлер-заглушка на любой незнакомый `callback_data` (шаг 4) | Без ответа Telegram крутит часики на кнопке около минуты — выглядит как зависший бот |
| Сообщение старше 48 часов не редактируется, действие всё равно записано (шаг 4) | Telegram отдаёт такое как `InaccessibleMessage`. Отказываться из-за этого от перехода статуса было бы хуже: человек нажал осознанно |
| Дев-токен — это `str(user.id)` без подписи и TTL (шаг 5a) | Настоящий Telegram Login Widget требует домен для `/setdomain`, которого на localhost нет (§12.4). Расширять эту схему до продакшен-авторизации не планируется — она размечена как дев-заглушка и в докстринге, и здесь |
| Кнопки веб-экрана (`can_start`/`can_snooze`/`can_complete`/`can_skip`) считает API, а не фронт (шаг 5a) | Инвариант 4: какое действие допустимо — бизнес-правило. Схема `OccurrenceOut` читает уже существующие наборы статусов из `services/occurrences` (`START_FROM`, `can_snooze()` и т.д.) — то же самое, что раньше делал только `ACTIONS` в хендлере бота |
| Доменные ошибки ловит один обработчик `ServiceError` в `app/api/main.py`, не try/except в каждом роутере (шаг 5a) | Аналог `explain()` в хендлерах бота: одно место решает, что `AlreadyInStatus`/`SnoozeLimitReached`/`InvalidTransition` — это 409, а `ValidationError` — 400. Роутеры остаются тонкими и не дублируют эту карту кодов |
| Прокси `/api` в `vite.config.ts` вместо CORS-мидлвари в FastAPI (шаг 5b) | Запрос уходит с dev-сервера Vite, а не из браузера, поэтому для браузера это тот же origin и preflight не нужен вовсе. Бэк остаётся без единой настройки, существующей только ради локальной разработки |
| Свой роутер на `location.hash`, без react-router (шаг 5b) | Пять экранов не окупают зависимость, а хост тут вообще без node — ставить пакет пришлось бы внутри контейнера, с обновлением lock-файла и долгой пересборкой образа. Хэш выбран вместо History API намеренно: dev-сервер Vite отдаёт `index.html` только по корню, и на F5 по адресу привычки понадобился бы фолбэк |
| Кнопки на «Сегодня» рисуются строго по флагам `can_*` из ответа (шаг 5b) | Продолжение решения шага 5a. Фронт не знает ни одного правила §4 — иначе веб и Telegram однажды разойдутся в том, что считают допустимым |
| Отказ действия (409) перечитывает весь список, а не только карточку (шаг 5b) | Отказ почти всегда значит, что статус уже сменился в другом месте: кнопкой в чате или джобом закрытия дня. Показать текст ошибки мало — на экране останутся кнопки, которых уже нет |
| Валидация формы не дублируется на фронте (шаг 5b) | Инвариант 4: непустое название, минимум 5 минут и диапазон дней проверяет `services/habits.py`. В форме только `required`/`min` как подсказка браузера, отказ показывается текстом бэка как есть |
| Расхождение таймзоны браузера и профиля показывается плашкой, а не правит профиль (шаг 5b) | Инвариант 2: источник локального времени один — `User.timezone`. Молча подменить его нельзя: человек мог уехать на неделю и не хотеть сдвигать расписание. Менять — руками в «Настройках» |
| Сетка heatmap строится от календаря, а данные раскладываются по ней (шаг 5b) | Ручка отдаёт только дни, на которые есть occurrence. Если рисовать подряд то, что пришло, дни вне расписания привычки съедут по неделям и картинка соврёт |
| Текст отправленного сообщения хранится снимком, а не пересобирается (шаг 6) | Пересборка через `services/messages.py` выглядела чище — тексты там чистые функции, — но occurrence изменяемый, и звали бы мы их на изменившемся объекте. Разошлись бы три вещи: ветка догоняющего пинга (`followup` смотрит на `status is in_progress`, а к моменту гашения статус терминальный), время в уведомлении (его двигает снуз) и название привычки (её правит §8). Прецедент в проекте уже есть и обоснован в §3 — `Occurrence.duration_minutes` тоже снимок, по той же причине. Побочно: Bot API не умеет прочитать чужое сообщение по `message_id`, так что текст обязан быть у нас в любом случае |
| `sent_messages` отдельной таблицей, а не колонками на `Occurrence` (шаг 6) | План в «Что дальше» предлагал две колонки под `message_id`. Но сообщений на занятие не два: по §5 снуз возвращает занятие в выборку диспетчера и обнуляет `followup_sent_at`, поэтому при пяти снузах уведомлений до шести и пингов до шести. Колонка перезаписывалась бы, и все сообщения, кроме последнего, остались бы в чате с живыми кнопками навсегда — та же находка 2, только с другой стороны |
| Диспетчер гасит вытесненные снузом сообщения сам, до `mark_notified` (шаг 6) | Джоб §6.4 их не видит: он смотрит на терминальный статус, а вытесненное сообщение принадлежит живому занятию. Гасить надо именно в момент отправки замены и именно до `mark_notified` — итог считается от текущего состояния, и пока статус `snoozed`, получается «⏰ Перенесено на 19:05», ровно как у кнопки в чате. После `mark_notified` вышло бы «Статус: notified» |
| Бот по-прежнему гасит сообщение сам, а не ждёт джоба (шаг 6) | Сообщение у него в руках, правка мгновенная — отдавать её джобу значило бы ухудшить то, что уже работает, ради единообразия. Джоб при этом всё равно нужен (действия из веба), и второе сообщение того же занятия достаётся ему |
| Отметка `closed_at` из бота — отдельной короткой транзакцией (шаг 6) | Ставить её надо после удачного редактирования, а оно происходит уже после `commit` перехода статуса. Если запись не дойдёт, джоб отредактирует сообщение тем же самым текстом и получит от Telegram «message is not modified» — это считается за успех, отметка встанет там. То есть худший исход — один лишний вызов API |
| Отказ Telegram при гашении считается окончательным (шаг 6) | `TelegramBadRequest` и `TelegramForbiddenError` при редактировании означают «сообщение старше 48 часов, удалено, не изменилось или бот заблокирован» — повтор на каждом тике копил бы одни и те же строки в логе вечно, потому что занятие уже терминальное и само из выборки не уйдёт. Временный сбой сети поднимает другое исключение и уходит на следующий тик |
| После `UPDATE` привычки роутер вызывает `session.refresh(habit)` перед ответом (шаг 5a) | `Habit.updated_at` — `onupdate=func.now()` на стороне Postgres. При `expire_on_commit=False` после `commit()` это поле не подтягивается само, и синхронный доступ Pydantic к нему на сериализации ловит `MissingGreenlet` (ленивый SELECT вне `await`). Поймано curl'ом на первом же `PATCH /habits/{id}` — без ручного `refresh()` эндпоинты `update_habit`/`archive_habit`/`restore_habit` падали 500 после успешного коммита |


### Решения по итогам сессии тестирования (26.08.2026)

Пятнадцать находок разобраны, по каждой принято решение и внесена правка в
`docs/requirements.md`. Все решения уже реализованы в коде (см. «Статус находок»).
Спека — источник истины, здесь только «почему так».

| Решение | Обоснование |
|---|---|
| `✅ Выполнил` добавлен в первое уведомление Telegram (находка 12) | Переход `notified → done` разрешён с шага 2, но кнопка была только в вебе, и одно занятие выглядело по-разному в двух интерфейсах вопреки §9. Равняем по вебу, а не наоборот: человек мог сделать зарядку и только потом посмотреть в телефон, отнимать у него отметку до догоняющего пинга незачем |
| Брошенный `in_progress` ночной джоб закрывает как `skipped`, не `missed` (находка 11) | §6.3 и §11 противоречили друг другу: первый велел вешать `missed` на всё нетерминальное, второй объявлял `missed` состоянием «не отреагировал вовсе» и потому неизменяемым. `started_at` заполнен — человек отреагировал. Та же логика, по которой «Не получилось» ведёт в `skipped`. Для серии разницы нет (§7 ломают одинаково), разница в честности статуса и в его неизменяемости |
| Снуз по-прежнему сбрасывает `followup_sent_at`, поправлена формулировка §5 (находка 10) | Пинг привязан к `current_due_at`, а снуз его двигает: уже отправленный «успел?» относится к отменённому сроку и права блокировать следующий не имеет. §5 сам говорит, что таймер пинга едет вместе со снузом. Читалось это как противоречие только потому, что решение жило комментарием в коде и в спеку не попало |
| Дев-вход не спрашивает таймзону, отличие записано в §12.4 (находка 14) | Пункт §9 про выбор таймзоны — про настоящий Login Widget, где человек заводится впервые. Дев-вход входит в сид-пользователя, у которого `timezone` уже стоит. Подставлять браузерную означало бы молча переписывать профиль при каждом входе — ровно то, от чего защищает плашка о расхождении на «Сегодня» (инвариант 2) |
| Смена таймзоны удаляет занятия с прошедшим новым временем, но предупреждает заранее (находка 3) | Оставить нельзя (диспетчер пришлёт уведомление в ту же секунду), молча удалить тоже: день исчезает из «Сегодня», из heatmap и из знаменателя процента. Значит удалять, но показывать в «Настройках» до сохранения, сколько занятий на сегодня исчезнет. Отдельная ручка предпросмотра, поведение сервиса не меняется |
| `change_user_timezone` пересчитывает `paused` вместе с `pending` (находка 4) | Пауза — отложенное будущее, а не история, и переезжать обязана вместе с расписанием. Одна правка в одном месте закрывает и дубли на один день, и 500 при возврате таймзоны. Альтернатива — чинить в `cancel_pause` — оставляла бы 500 достижимым по другим путям |
| `IntegrityError` ловится общим обработчиком и отдаёт 409 (находка 5) | Уникальный индекс `(habit_id, scheduled_at)` — это бизнес-правило «одно занятие на привычку и время», а не сбой инфраструктуры. Сейчас он пролетает мимо обработчика `ServiceError` и превращается в голый 500. Подстраховка на будущее: причину дублей чиним отдельно, но пользователь не должен видеть трейсбек, если она вернётся |
| Архивация закрывает открытые занятия текущего дня как `skipped` (находка 7) | Архив по §8 — осознанный отказ от привычки, тот же жест, что и «Не получилось». Без этого занятие оставалось живым: пинги продолжали приходить, карточка висела на «Сегодня», а привычки в списке уже не было, и вернуть её из архива человек не мог. К ночи занятие уходило в `missed` и портило процент уже брошенной привычки |
| Закрытие дня пропускает записи с `current_due_at` в будущем (находка 6) | Снуз незадолго до полуночи переносит срок в новые сутки, а `local_date` остаётся вчерашним — джоб убивал занятие раньше обещанного кнопкой срока. Занятие остаётся за своим днём в статистике, следующий часовой прогон закроет его, если срок прошёл, а ответа не было. Двигать `local_date` вместе со снузом отвергли: во вчерашнем дне осталась бы дыра, а в новом могло уже быть своё занятие той же привычки |
| Дни паузы в heatmap берутся из `HabitPause`, а не из occurrences (находка 9) | Генератор дни активной паузы пропускает, поэтому `paused` получают только занятия, успевшие появиться до постановки паузы — на горизонте в двое суток. Для паузы в двадцать дней это два дня из двадцати. Occurrence остаётся записью о запланированном выполнении, а пауза — факт о привычке и живёт в своей таблице; сетку строит ручка. Заставлять генератор писать `paused` отвергли: прошедшие дни длинной паузы это всё равно не закрасило бы |
| Пауза задним числом запрещена (находка 8) | Интервал в прошлом длиннее 14 дней обнулял уже сложившуюся серию (проверено: 11 → 5), при том что дни внутри давно закрыты своими статусами и продолжают считаться. То есть правило §7 применялось только штрафной половиной. §10 выносит ретроспективную отметку за MVP — ограничение действует независимо от того, через какую ручку она пришла |
| Пауза видна на «Моих привычках» меткой, форма заморозки остаётся на экране привычки (находка 13) | §9 перечисляет паузу среди содержимого списка, но форма с датами, предупреждением про 14 дней и текущей серией осмысленна только там, где эта серия показана. Дублировать предупреждение в двух местах — верный способ однажды поправить его в одном |
| Список пауз показывает только незакончившиеся (находка 15) | `active_pauses` фильтрует по `cancelled_at IS NULL` и не смотрит на дату окончания, поэтому «снять досрочно» предлагается и для паузы, закончившейся месяц назад. Данные это не портит, но список растёт бесконечно |

## План работ

1. ~~**Каркас** — compose, Dockerfile'ы, конфиг, модели, первая миграция~~ ✅
   Критерий: `docker compose up` работает, таблицы созданы
2. ~~**Сервисный слой** — переходы статусов, расчёт стриков, фикстуры~~ ✅
   Критерий: `pytest` зелёный, `seed` считает серии верно
3. ~~**Планировщик** — три джоба, отправка замокана логами~~ ✅
   Критерий: в логах воркера виден полный цикл occurrence
4. ~~**Бот** — хендлеры кнопок, реальная отправка~~ ✅
   Критерий: уведомление приходит в Telegram, кнопка меняет статус в БД
5. **Фронт** — разбит на две сессии:
   - ~~**5a. API** — роутеры под §9, схемы, `POST /auth/dev-login` (§12.4)~~ ✅
     Критерий: `curl` по всем эндпоинтам отдаёт то, что ждут экраны раздела 9
   - ~~**5b. Экраны** — React из раздела 9 поверх готового API~~ ✅
     Критерий: привычку можно завести, отметить и увидеть в статистике, не заходя в psql
6. ~~**Синхронизация чата** — `sent_messages`, четвёртый джоб (находка 2)~~ ✅
   Критерий: начал в Telegram, закончил в вебе — оба сообщения в чате гаснут за один тик

Шага 6 в исходном плане не было: он появился из находки 2 после того, как фронт заработал и стало видно, что чат про действия в вебе не знает.

Порядок важен: приложение целиком живёт в джобах планировщика, фронт — последний.

Почему шаг 5 разбит надвое: в плане он записан как «фронт», но API под него нет — в `app/api/main.py` только `/health`. Писать экраны против несуществующих эндпоинтов означает переделывать их дважды. И у 5a есть свой проверяемый критерий, не требующий ни строчки React.

## Что нужно от пользователя перед стартом

- [x] Docker Desktop установлен, `docker compose version` работает
- [x] Токен бота от @BotFather получен
- [x] Свой Telegram ID узнан через @userinfobot, лежит в `.env` как `SEED_TELEGRAM_ID`
- [x] `.env` создан из `.env.example`, `.env` в `.gitignore`
- [x] `SEED_TIMEZONE=Europe/Moscow` в `.env` — таймзона заказчика. От неё зависит `local_date`, а значит и то, в какой день попадут стрики
- [x] **Написать своему боту `/start` в Telegram.** Без этого отправка не работает: Telegram запрещает писать пользователю, который сам не начал диалог. Проверено на шаге 4 — сообщения доходят

## Что стоит помнить про API

- **API — такой же тонкий адаптер, как бот и джобы (инвариант 4).** Одно действие ведёт себя одинаково из веба и из Telegram: «Выполнил» на экране «Сегодня» зовёт ту же `occurrences.complete`, что и кнопка в чате. Новой бизнес-логики в `app/api/` нет ни строчки.
- **Выборки под экраны:** `occurrences.list_for_local_date` — «Сегодня», `habits.habits_of_user` — «Привычки», `stats.habit_stats` и `stats.heatmap` — «Статистика», `habits.find_overlaps` — предупреждение о пересечении из §9.
- **Локальная дата берётся из `habits.user_today`, а не из браузера.** Инвариант 2: единственный источник локального времени — `User.timezone`.
- **Вход:** при `DEV_AUTH=true` — `POST /auth/dev-login` на сид-пользователя. Пользователя ищет `users.get_by_telegram_id`.
- **Полный список ручек — `/docs`**, Swagger FastAPI поднимает сам на `localhost:8000/docs`. Заголовки, описания и коды ошибок дописаны руками: автоматически в схему попадали только успешный ответ и 422, а 400/404/409 рождаются в обработчиках `app/api/main.py` и в зависимостях, откуда FastAPI их не видит. Описания кодов лежат в `app/api/responses.py` — одним списком, а не по декораторам, потому что задаёт их одна таблица «доменная ошибка → HTTP» в `service_error_handler`.
- **Схему проверяет `tests/test_openapi.py`.** Не FastAPI ради FastAPI: кривой `responses` не роняет ни импорт, ни эндпоинт — приложение поднимается, тесты зелёные, и только `/docs` отдаёт 500. Заметить это без теста можно лишь случайно, зайдя на страницу.

## Состав фронта

Всё под `frontend/src/`. Зависимостей ровно две — `react` и `react-dom`; ни роутера, ни клиента запросов, ни библиотеки состояния тут нет, и `package-lock.json` с шага 1 не менялся.

| Файл | Что внутри |
|---|---|
| `api/types.ts` | зеркало `app/api/schemas.py` — меняется схема на бэке, меняется и здесь |
| `api/client.ts` | обёртка над `fetch`: токен в `localStorage`, разбор `{"detail": ...}`, `ApiError` с кодом |
| `router.ts` | `useRoute`/`navigate` на `location.hash` |
| `format.ts` | время по `User.timezone`, дни недели, окно привычки, названия статусов |
| `App.tsx` | загрузка профиля по токену, навигация, плашка о расхождении таймзоны |
| `screens/` | пять экранов раздела 9 |
| `components/HabitForm.tsx` | форма создания и правки с проверкой пересечений |
| `components/Heatmap.tsx` | сетка 90 дней |
| `styles.css` | всё оформление, цвета статусов — CSS-переменные |

Что стоит помнить, если возвращаться к фронту:

- **Браузер не ходит на `localhost:8000` напрямую.** Все запросы идут на `/api/...`, префикс снимает прокси в `vite.config.ts`. Поэтому CORS в FastAPI не настроен и не нужен — если однажды фронт станет собираться в статику и отдаваться отдельно, прокси исчезнет и CORS понадобится.
- **Токен лежит в `localStorage` и переживает перезагрузку.** Но `python -m app.fixtures.seed` пересоздаёт пользователя с новым `id`, и старый токен становится чужим. Клиент ловит 401, чистит токен и возвращает на экран входа — если после сида фронт вдруг «разлогинился», это оно.
- **Экран «Сегодня» показывает только сегодня.** Ручка умеет `?day=`, но §9 просит список на сегодня, и переключателя дней намеренно нет.
- **У `pending` все `can_*` — false, и кнопок на карточке нет.** Так и задумано (§4): действия появляются после того, как диспетчер отправил уведомление. На свежем сиде большинство занятий выглядит именно так — это не сломанный экран.
- **Текст 409 показывается как есть.** На двойное нажатие приходит «действие 'complete' недопустимо из статуса 'done'» — формулировка внутренняя, но понятная; смягчать её значило бы править сервисный слой. Бот в том же случае отвечает мягче через `messages.action_note`.
- **`tsconfig.json` включает `ES2022.Intl`** — ради `Intl.supportedValuesOf('timeZone')` в списке таймзон на «Настройках».

## Как отлаживать бота

- **Поставь `SCHEDULER_TICK_SECONDS=5` в `.env`.** По умолчанию 60. После отладки верни обратно, иначе тик в пять секунд будет молотить всё время.
- **Сид ставит «Отжимания» на три минуты вперёд:** `docker compose exec api python -m app.fixtures.seed`, дальше `docker compose logs -f worker bot`.
- **Сид пересоздаёт пользователя.** Каскад по FK уносит вместе с ним все привычки и историю, так что нажатия, сделанные до сида, в БД не останутся.
- **Чтобы уведомления ушли в лог, а не в чат** — очисти `TELEGRAM_BOT_TOKEN`: воркер сам переключится на `LogNotifier`.
- **Смотреть состояние:** `docker compose exec db psql -U postgres -d habits -c "select h.title, o.status, o.snooze_count from occurrences o join habits h on h.id=o.habit_id where o.local_date = current_date;"`

## Находки ручного тестирования

Первые две были про то, что состояние живёт в БД, а чат про это состояние ничего не знает. Дальше — находки сквозной сессии тестирования против §4, §5, §7 и §9.

### 1. Непонятно, что надо подписаться на бота — закрыто дёшево

Telegram не даёт боту написать первым, пока человек не отправил `/start`. Попытка возвращает `TelegramForbiddenError`, а в диспетчере он ловится тем же `except Exception`, что и мигнувшая сеть ([jobs.py:46](backend/app/scheduler/jobs.py)): `notified_at` не проставляется, и следующий тик пробует снова — так до конца дня, пока джоб закрытия не переведёт занятие в `missed`. Человек не получает ничего и нигде об этом не узнаёт: единственный след — логи воркера.

Закрыто фронтом, без миграций: инструкция в «Настройках» и одноразовая подсказка на «Сегодня» с закрытием через `localStorage`. Проверить программно, начал ли человек диалог, нельзя — Telegram про это не сообщает, поэтому подсказка показывается всем и закрывается вручную.

Что осталось несделанным и было бы правильнее: диспетчер должен отличать `TelegramForbiddenError` (повторять бессмысленно, надо сказать человеку) от временного сбоя (надо повторить). Тогда плашка показывалась бы только тем, у кого связь реально не работает. Это требует колонки на `User` и миграции.

### 2. Действие в вебе не отражается в чате — закрыто шагом 6

Начал упражнение кнопкой в Telegram, закончил в вебе — сообщение в чате осталось с живыми кнопками и выглядело как «ещё не отвечено».

Причина: бот дописывал итог в `_close_message` ([handlers.py](backend/app/bot/handlers.py)), работая с `callback.message` — сообщение у него в руках, потому что нажатие пришло оттуда же. Когда действие приходило из веба, дотянуться до сообщения не мог никто: `message_id` нигде не хранился.

**Сообщений при этом два, а не одно** — первое уведомление и догоняющий пинг, у каждого свой набор живых кнопок. Данные не ломались: нажатие на протухшую кнопку возвращало `AlreadyInStatus`, бот отвечал репликой. Врал только чат.

Дёшево это и не починилось: понадобились таблица `sent_messages`, миграция `0003`, два новых метода у `Notifier` и четвёртый джоб. Подробности — в разделе «Что сделала сессия «синхронизация чата»».

### Статус находок

**Все пятнадцать разобраны, решения приняты, спека обновлена** (26.08.2026,
та же сессия). Что решили и почему — в таблице «Решения по итогам сессии
тестирования» выше; что теперь считается правильным поведением — в
`docs/requirements.md`, он остаётся источником истины.

Закрыты 3, 4, 5, 8, 9 (сессия «таймзона и паузы») и 6, 7, 11, 12 (сессия
«где кончается занятие»), обе 26.08.2026, плюс 10 и 14, которые закрылись
правкой спеки. Сессия «фронт» (27.08.2026) закрыла 13 и 15, сессия
«синхронизация чата» того же дня — находку 2, самую дорогую из всех.
Открытых находок не осталось.

| Находка | Решение | Где чинить |
|---|---|---|
| ~~2 действие в вебе не видно в чате~~ ✅ | хранить `message_id` и текст, гасить джобом | `sent_messages`, §6.4 |
| ~~3 смена таймзоны удаляет занятия~~ ✅ | удалять, но предупреждать заранее | `GET /users/timezone-preview` + «Настройки» |
| ~~4 `paused` не пересчитывается~~ ✅ | пересчитывать вместе с `pending` | `change_user_timezone` |
| ~~5 500 и залипший профиль~~ ✅ | снято починкой 4; `IntegrityError` → 409 | `app/api/main.py` |
| ~~6 снуз через полночь~~ ✅ | закрытие дня щадит будущие сроки | §6.3, `close_local_day` |
| ~~7 архив не гасит занятие~~ ✅ | закрывать открытые как `skipped` | §8, `archive_habit` |
| ~~8 пауза задним числом~~ ✅ | запретить `starts_on` в прошлом | `pause_habit` |
| ~~9 пауза мимо heatmap~~ ✅ | достраивать дни паузы из `HabitPause` | `stats.heatmap` |
| 10 повторный догоняющий пинг | код прав, поправлена §5 | закрыто правкой спеки |
| ~~11 брошенный `in_progress`~~ ✅ | `skipped`, не `missed` | §6.3, `close_local_day` |
| ~~12 четыре кнопки против трёх~~ ✅ | добавить «Выполнил» в Telegram | §5, `messages.reminder` |
| ~~13 пауза не видна в списке~~ ✅ | метка на карточке | §9, фронт |
| 14 таймзона на входе | отличие дев-входа записано в §12.4 | закрыто правкой спеки |
| ~~15 «снять досрочно» у старой паузы~~ ✅ | фильтровать по дате окончания | `habits.active_pauses` |

### Что сделала сессия «таймзона и паузы» (26.08.2026)

Пять коммитов, по одному на находку, плюс шестой на формулировку
предупреждения. `pytest` зелёный: 103 обычных теста и 4 оставшихся xfail
(находки 6, 7, 11, 12). `tsc` и `npm run build` проходят.

| Что изменилось | Где |
|---|---|
| `pending` и `paused` пересчитываются вместе; расчёт «что станет с занятиями» вынесен в `_plan_timezone_change` | `services/habits.py` |
| `preview_timezone_change` — тот же расчёт, но без записи; за ним ручка `GET /users/timezone-preview` и предупреждение в «Настройках» | `services/habits.py`, `api/routers/users.py`, `screens/SettingsScreen.tsx` |
| `IntegrityError` → 409 отдельным обработчиком, конфликт уходит в лог целиком | `api/main.py` |
| `pause_habit` отбивает `starts_on` раньше сегодняшнего дня по таймзоне владельца; на форме заморозки `min` у поля даты | `services/habits.py`, `screens/HabitScreen.tsx` |
| `stats.heatmap` достраивает дни паузы из `HabitPause`; принимает `habit` и `tz` вместо `habit_id` | `services/stats.py`, `api/routers/habits.py` |

Решения, которые пришлось принять по ходу — спека их не оговаривает:

- **Занятие в heatmap сильнее паузы.** Пауза заполняет только дни, на которых
  записи нет. Иначе день, закрытый как `done` живым занятием (`pause_habit`
  намеренно не трогает `notified`/`snoozed`/`in_progress`), перекрасился бы
  в `paused` — то есть реальное выполнение стёрлось бы из календаря.
- **Дни вне расписания привычки паузой не красятся.** Пауза на 20 дней у
  привычки по пн/ср/пт закрашивает только пн/ср/пт: без паузы этих дней
  в календаре тоже не было бы, и красить их значило бы соврать про расписание.
- **`paused` с прошедшим новым временем удаляется так же, как `pending`.**
  Основание §8 («диспетчер пришлёт уведомление в ту же секунду») для паузы
  срабатывает отложенно — в момент её снятия, но срабатывает.
- **`removed_today` считает только сегодняшний день**, как и просит §8.
  При сдвиге больше суток теоретически может исчезнуть и завтрашнее занятие;
  на горизонте генерации в двое суток это требует переезда почти через
  датную линию поздним вечером. Если такой случай всплывёт — считать по всему
  плану, поле уже отдаётся отдельной ручкой.
- **Таймзону для «сегодня» `pause_habit` берёт из владельца сама**, через
  `session.get(User, habit.user_id)`, а не из аргумента: вызывающих у неё
  трое (роутер, фикстуры, тесты), и «сегодня» у всех должно быть одно.

Проверено на живом стенде: переезд туда-обратно без дублей, предпросмотр
и heatmap паузы совпадают с ожиданием, пауза задним числом — 400.

**Две ловушки в тестах, оставшиеся с прошлой сессии.** Починены 27.08.2026
сессией «синхронизация чата» — к тому дню они уже покраснели, как и было
предсказано. Ниже то, как это выглядело; чинились оба, как и написано, —
передачей `now`.

Они были зелёными, но привязаны к календарной дате 26.08.2026 и к реальным
часам, а не к переданному `now`:

- `test_предпросмотр_смены_таймзоны_показывает_число_удаляемых_занятий`
  (`test_api_users.py`) — занятие на 19:00 по Москве 26.08 удаляется по Токио
  только после 10:00 UTC. До этого часа и в любой другой день тест покраснеет;
- `test_пауза_без_даты_окончания_ставится_на_неделю` (`test_habits_db.py`) —
  зовёт `pause_habit` без `now` со `starts_on=26.08.2026`. С гардом находки 8
  назавтра это станет паузой задним числом.

Трогать их та сессия не стала намеренно: правка теста ради зелёного цвета —
именно то, чего просили не делать. Чинятся оба одинаково: передать `now`
и считать даты от него.

### Что сделала сессия «где кончается занятие» (26.08.2026)

Четыре коммита, по одному на находку. `pytest` — 108 тестов, зелёный,
xfail'ов не осталось ни одного.

| Что изменилось | Где |
|---|---|
| `close_local_day` пропускает записи с `current_due_at` в будущем — снуз за полночь доживает до своего срока (находка 6) | `services/occurrences.py` |
| Тот же джоб стал двумя переходами: `pending`/`notified`/`snoozed` → `missed`, `in_progress` → `skipped` (находка 11) | `services/occurrences.py` |
| `archive_habit` гасит открытые занятия сегодняшнего дня как `skipped`; таймзону владельца берёт сама (находка 7) | `services/habits.py`, `services/occurrences.py` |
| `messages.reminder` добавляет «✅ Выполнил» — набор кнопок совпал с вебом (находка 12) | `services/messages.py` |

Решения, которые пришлось принять по ходу:

- **Правило «во что закрывать день» живёт в `DAY_CLOSE_TRANSITIONS`,
  а не в `WHERE`.** После находки 11 массовый UPDATE стал двумя разными
  переходами, и было соблазнительно развести их условием прямо в SQL.
  Инвариант 4 распространяется и на массовые операции: статус occurrence
  решается только в `services/occurrences.py`, поэтому запросов два, а список
  переходов — обычная константа модуля, которую видно глазами.
  `UNRESOLVED_STATUSES` теперь собирается из неё, а не наоборот.
- **`mark_missed` сузился до `MISS_FROM`.** Одиночный аналог джоба не должен
  уметь то, чего джоб больше не делает, иначе `in_progress → missed` вернётся
  через первый же новый вызов. В production-коде у функции вызывающих нет,
  поправлен только `test_transitions.py`.
- **`current_due_at <= at` применяется ко всем четырём статусам, не только
  к `snoozed`.** Спека формулирует оговорку без исключений, а у остальных
  статусов срок в будущем и так не встречается — отдельная ветка была бы
  кодом на случай, которого нет.
- **Архивация сначала удаляет будущие `pending`, потом гасит остаток.**
  В обратном порядке занятие на вечер того же дня получило бы `skipped`
  вместо удаления — то есть в статистику попал бы отказ, которого человек
  не совершал.
- **`paused` архивация не трогает.** Приостановленный день по §7 в расчётах
  не участвует и пингов не порождает; превращать его в отказ незачем.

Проверено на живом стенде в ночь на 27.08.2026 — все четыре критерия
подтверждены (снуз за полночь доживает до срока, брошенный `in_progress`
уходит в `skipped`, кнопки в чате и вебе совпали, архивация гасит пинги).

**Две ловушки живой проверки, обе стоили одного сорванного прогона.**

- **Часы машины автора опережают `Europe/Moscow` на два часа**, а сид живёт
  по Москве. Время в инструкциях, набранное с настенных часов, промахивается:
  привычка, заведённая «на 21:45», получила расписание в прошлом, занятие
  на сегодня не создалось, и уведомления никто не дождался. Все моменты
  в сценариях проверки считать от `now()` в базе
  (`(now() AT TIME ZONE 'Europe/Moscow')::date`, `now() + interval '1 minute'`),
  а не записывать числом.
- **Диспетчер с тиком в 5 секунд успевает влезть между подготовкой данных
  и ручным запуском джоба.** Занятие, которому подвинули `current_due_at`
  в прошлое, он перехватывает и шлёт уведомление. На результат это не влияет,
  но если готовите состояние и проверяете джоб — сцепляйте `psql` и запуск
  в одну команду, а `followup_sent_at` заполняйте сразу.

### Что сделала сессия «фронт» (27.08.2026)

Два коммита на находки, третий не по плану. `pytest` — 110 тестов, зелёный
(добавлены два теста на находки 13 и 15).

| Что изменилось | Где |
|---|---|
| `active_pauses` фильтрует ещё и по `ends_on >= today` — пауза, закончившаяся давно, больше не предлагает «снять досрочно» (находка 15) | `services/habits.py`, `api/routers/habits.py` |
| Новая `paused_until_today` одним запросом находит привычки, у которых пауза активна сегодня, и отдаёт дату окончания; `HabitOut.paused_until` заполняет его только в `GET /habits` (находка 13) | `services/habits.py`, `api/schemas.py`, `api/routers/habits.py` |
| Карточка на «Мои привычки» рисует метку «на паузе до …» тем же цветом, что и `paused` в heatmap; форма заморозки осталась на экране привычки, как и решили | `screens/HabitsScreen.tsx` |

Не по плану, отдельным запросом в той же сессии: инструкция «откройте в
Telegram своего бота» на «Настройках» не была ссылкой. Добавлена
`TELEGRAM_BOT_USERNAME` — юзернейм без `@`, задаётся вручную (Telegram не
отдаёт его по токену без похода в API бота, а заводить это ради статичной
строки в api-процессе незачем). Подмешивается в `UserOut` через новую
`user_out()`, а не только в `get_me`: иначе `PATCH /users/me` при смене
таймзоны стирал бы ссылку из состояния фронта до следующей перезагрузки.
Без переменной в `.env` остаётся прежний текст со ссылкой на `TELEGRAM_BOT_TOKEN`.

Проверено живьём в браузере на реальном сиде и боте — оба сценария и ссылка работают как задумано.

Замечено и не относится к правке: `occurrencesWord is not defined` в консоли на `SettingsScreen` — залипший кэш dev-сервера Vite (такой строки в исходниках нет), ушло после `docker compose restart web`.

### Что сделала сессия «архитектурная документация» (27.08.2026)

Два коммита. Кода не тронуто ни строчки — только `docs/`.

| Что появилось | Где |
|---|---|
| C4 уровня контекста и контейнеров, четыре сиквенса, три «решения, которых на диаграммах не видно», таблица «что намеренно не нарисовано» | `docs/architecture.md` |
| ER-диаграмма переведена с Mermaid на PlantUML, пояснения к полям вынесены в `note`, раздел ограничений разложен на CHECK / индексы / enum | `docs/erd.md` |

**Формат сменился на PlantUML по ходу сессии, и у этого есть цена.**
Изначально всё рисовалось на Mermaid (так записано в `PROMPTS.md`), потом
заказчик попросил PlantUML. Плата: **GitHub PlantUML в markdown не рендерит**,
на вебе GitHub блоки видны исходником. Взамен C4 рисуется настоящей
библиотекой C4-PlantUML из stdlib (`!include <C4/C4_Container>`) вместо
прямоугольников, стилизованных руками, а к сущностям ER можно прицепить
`note` — Mermaid не умеет ни того, ни другого. Раз формат сменился,
`erd.md` переведён тоже: два формата в одной папке хуже любого одного.
Чем смотреть картинками — в разделе «Про формат» в начале `architecture.md`.

**Диаграммы проверены рендером, а не глазами.** `plantuml.jar` прогнан по
обоим файлам (`java -jar plantuml.jar -tsvg docs/architecture.md`), все семь
диаграмм собираются без ошибок. Это не формальность: PlantUML на синтаксической
ошибке не падает, а молча рисует картинку с текстом ошибки, и в редакторе это
можно месяц не заметить. Проверка сразу нашла, что нумерация шагов в подписях
под сиквенсами разъехалась с той, что `autonumber` проставляет на самом деле —
поправлено в четырёх местах.

Решения о том, чего **не** показывать (это и была основная работа):

- **Компонентного уровня C4 нет.** Внутри бэкенд-образа один архитектурно
  значимый факт — «адаптеры тонкие, решения в `services/`», и фразой он
  выражается полнее, чем схемой из десяти прямоугольников по числу модулей
  `services/`. Появится второй внешний интерфейс или второй `Notifier` —
  уровень станет осмысленным.
- **Цикл уведомления не доведён до нажатия кнопки.** В промпте они шли одной
  цепочкой, но между отправкой и нажатием проходит от секунды до часов: это
  два независимых события, и склеенный сиквенс врал бы про связность.
  Диаграммы две, в первой стоит явная отсылка ко второй.
- **`Notifier` оставлен участником сиквенса**, хотя это не сетевая граница,
  а протокол. Без него с диаграммы не читается инвариант 5, а с ним — и то,
  почему при пустом `TELEGRAM_BOT_TOKEN` весь цикл работает в лог.
- **Восемь сценариев не нарисованы**, каждый с причиной в таблице в конце
  файла: CRUD, снуз и остальные кнопки, статистика, смена таймзоны, пауза,
  Login Widget, порядок старта контейнеров.

Три вещи, которые иначе забудутся, записаны отдельным разделом до сиквенсов,
со ссылками на строки кода: почему планировщик отдельным процессом, почему
диспетчер сначала отправляет и лишь потом ставит `notified_at` (таблицей
«что будет при сбое» для обоих порядков), чем `bot` отличается от `worker`
в разговоре с Telegram.

Расхождений код↔спека, требующих решения, не нашлось: всё, что расходится,
уже разобрано и записано раньше. Одно место спека просто не описывает —
`create_habit` зовёт `generate_for_habit` синхронно, — и это как раз третий
сиквенс.

`PROMPTS.md` и `PARALLEL-AGENTS.md` намеренно не правились: там записано,
что просили в тот момент, а не что получилось. Смена формата — факт этой
сессии, и живёт он здесь.

### Что сделала сессия «синхронизация чата» (27.08.2026)

Шаг 6, находка 2. Девять коммитов: слой за слоем, потом документация,
потом не по плану — две ловушки в тестах. `pytest` — 119 тестов, зелёный.

Развилка, оставленная нерешённой в прошлой сессии («хранить текст или
пересобирать»), разобрана до кода и решена в пользу хранения. Обоснование —
в таблице решений выше, там же и поправка к плану: не колонки, а таблица.

| Что изменилось | Где |
|---|---|
| Таблица `sent_messages` (`kind`, `message_id`, `text`, `sent_at`, `closed_at`) и тип `sent_message_kind` | `models/sent_message.py`, миграция `0003` |
| `Notifier.send` возвращает `message_id`, добавился `Notifier.close` | `notifier.py`, `telegram.py` |
| `services/sent_messages.py`: `record`, `mark_closed`, `open_for_occurrence`, `find_open`, `due_for_closing` | `services/` |
| `messages.closed_text` — единственное место, где снимок склеивается с итогом | `services/messages.py` |
| Четвёртый джоб `close_chat_messages`, диспетчер записывает отправленное и гасит вытесненное снузом | `scheduler/jobs.py`, `scheduler/main.py` |
| Бот гасит сообщение снимком текста и ставит `closed_at` | `bot/handlers.py` |

**Критерий проверен на живом стенде.** Занятие переведено в `in_progress`,
диспетчер прислал догоняющий пинг (два живых сообщения в чате), после чего
занятие закрыто через `POST /occurrences/{id}/complete`. Действие в
09:34:55.17, оба сообщения погашены в 09:34:55.95 — меньше чем за тик,
в логе `закрытие сообщений: погашено 2`. Отдельно проверено вытеснение
снузом: снуз из веба, затем повторное уведомление — в логе
`погашено вытесненных 1`, у прошлой строки `closed_at` проставлен.

Что стоит помнить:

- **`LogNotifier` возвращает `None`, и записи в `sent_messages` не появляется.**
  Это не баг: строчку в логе гасить нечем. Но значит, что при пустом
  `TELEGRAM_BOT_TOKEN` весь механизм шага 6 в логах не виден вовсе —
  проверять его можно только с настоящим ботом.
- **Сообщения, отправленные до миграции `0003`, остались с живыми кнопками.**
  Их `message_id` не сохранился нигде, погасить их нечем. Нажатие на них
  по-прежнему работает, и бот отредактирует такое сообщение сам — для этого
  случая в `_apply` оставлен фолбэк на `message.html_text`.
- **Занятия, закрытые кнопкой в чате, тоже проходят через джоб** — не то
  сообщение, на котором нажали (его гасит бот сразу), а второе. Поэтому
  «в логе после нажатия появилась строка про гашение» — норма, а не признак
  того, что бот не отработал.

Не по плану, отдельным коммитом в конце: починены две ловушки в тестах,
привязанные к календарной дате 26.08.2026. Прошлая сессия оставила их
намеренно, но 27-го числа они действительно покраснели. `pause_habit`
теперь получает `now` явно; в тесте предпросмотра таймзоны ручка читающая
и `now` в неё не передать, поэтому данные строятся от `now_utc()`, а не от
календарного числа — расписание берётся равным «час назад по Токио», и это
верно в любой момент суток. Проверено намеренной поломкой: со сдвигом
расписания в будущее тест падает.

## Что дальше

Готовые промпты под эти сессии лежат в `PROMPTS.md` — там же порядок запуска и какую модель на что брать. Здесь остаётся «что и почему», там — «с чего начать».

План MVP пройден целиком, находок не осталось. Незакрытыми пунктами остаются
два последних: тесты на фронт и §10. Дальше — по убыванию пользы:

- ~~**Тесты на роутеры — перед починкой находок, а не после.**~~ ✅ Готово.
  `backend/tests/conftest.py` поднимает отдельную БД `habits_test` в
  контейнере `db` (не ту, на которой сидит ручной сид) настоящими
  alembic-миграциями, один раз на сессию pytest; каждый тест получает
  сессию на SAVEPOINT внутри общей транзакции, откат после. Добавлено:
  `test_habits_db.py` (`change_user_timezone`, `cancel_pause`,
  `pause_habit`, `archive_habit`, `restore_habit`), `test_occurrences_db.py`
  (`close_local_day`), `test_generation_db.py` (`generate_for_habit`,
  `regenerate`), `test_api_*.py` (роутеры: коды ответов, 404 на чужой
  ресурс, доменные ошибки → HTTP), плюс один тест в `test_messages.py`.

  Находки 3, 4, 6, 7, 8, 9, 11, 12 закреплены как `xfail(strict=True)` на
  РЕШЁННОЕ поведение из `docs/requirements.md` — код его ещё не
  реализует. Находка 5 (`IntegrityError` → 500) в этот список не входит:
  она в `app/api/main.py`, а не в восьми проверенных функциях, и всплывёт
  сама, когда починка находки 4 уберёт дубли, которые её вызывают.
  Сессии починки снимают пометки по одной — `pytest -rxX` их перечисляет.
  99 обычных тестов + 8 xfailed, `pytest` зелёный.

  Сессия «таймзона и паузы» сняла четыре пометки (находки 3, 4, 8, 9) и
  добавила свой тест на находку 5 — `IntegrityError` действительно всплыл
  сам, как и было предсказано. Сессия «где кончается занятие» сняла
  оставшиеся четыре (находки 6, 7, 11, 12). Сейчас 108 обычных тестов,
  xfail'ов нет. Ни один тест ради зелёного цвета не переписан; наборы
  кнопок в `test_messages.py` поправлены потому, что кнопок стало четыре
  по решению находки 12, а не потому, что тест мешал.

  Проверено: намеренная поломка `archive_habit` (закомментирован вызов
  `delete_future_pending`) уронила `test_архивация_удаляет_будущие_pending_и_сохраняет_историю`
  — правка тут же откатана, `git diff` по `app/` пуст.

- ~~**Сессия «таймзона и паузы»: находки 3, 4, 5, 8, 9.**~~ ✅ Готово.
  Все четыре критерия проверены на живом стенде, подробности — в разделе
  «Что сделала сессия «таймзона и паузы»» выше.

- ~~**Сессия «жизненный цикл занятия»: находки 6, 7, 11, 12.**~~ ✅ Готово.
  Критерий выполнен: занятие, отложенное снузом за полночь, доживает до своего срока; архивация гасит сегодняшнее занятие и пинги прекращаются; наборы кнопок в вебе и в чате совпадают. Подробности — в разделе «Что сделала сессия «где кончается занятие»» выше.

- ~~**Сессия «фронт»: находки 13 и 15.**~~ ✅ Готово.
  Метка «на паузе до …» на карточке в «Моих привычках», фильтр по дате окончания в списке пауз. Подробности — в разделе «Что сделала сессия «фронт»» выше.

- ~~**Шаг 6: синхронизация чата (находка 2).**~~ ✅ Готово.
  Критерий выполнен: действие в вебе гасит и первое уведомление, и догоняющий
  пинг в течение одного тика. Развилка «хранить текст или пересобирать»
  решена в пользу хранения, план поправлен с колонок на таблицу — подробности
  в разделе «Что сделала сессия «синхронизация чата»» выше.
- ~~**Документация:** README, ERD, C4 и сиквенсы.~~ ✅ Готово.
  `README.md`, `docs/erd.md`, `docs/architecture.md`. Подробности — в разделе
  «Что сделала сессия «архитектурная документация»» ниже.
- **Тесты на фронт.** Роутеры закрыты — `test_api_auth.py`, `test_api_habits.py`,
  `test_api_occurrences.py`, `test_api_users.py` (см. пункт «Тесты на роутеры ✅»
  выше). Непокрытым остался только `frontend/src/`: в `package.json` нет ни
  `vitest`, ни `@testing-library`, единственная автоматическая проверка — `tsc -b`
  внутри `npm run build`, то есть типы, а не поведение. Экраны проверены руками,
  и следующая правка это не поймает.

  Прежняя формулировка этого пункта («тесты на роутеры и фронт, 49 юнит-тестов
  покрывают только сервисный слой») пережила сессию, которая роутеры закрыла,
  и два экрана противоречила пункту выше. Поправлено 27.08.2026.
- **Диспетчер не отличает `TelegramForbiddenError` от временного сбоя.**
  Хвост находки 1: человек, не написавший боту `/start`, не получает ничего
  и нигде об этом не узнаёт, а джоб пробует снова каждый тик до конца дня.
  Требует колонки на `User` и миграции. Шаг 6 этого не изменил: в гашении
  сообщений такой отказ считается окончательным, а в отправке — по-прежнему
  нет, и это осознанная разница (отправку повторить осмысленно, правку
  сообщения, которое человек всё равно не увидит, — нет).
- **Раздел «Не входит в MVP» (§10)** — трогать осознанно и по одному.

Сессия начинается с `/clear` в том же окне — рабочая папка и настройки сохранятся. Модель по умолчанию Sonnet, `/model opus` действует только на одну сессию.

Первый промпт:

> В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md.
> Прочитай их. План MVP пройден, хочу <что именно>.
