"""Числа из спеки в одном месте. Магических констант по коду быть не должно."""

from datetime import timedelta

# §3: длительность привычки, минимум и значение по умолчанию.
MIN_DURATION_MINUTES = 5
DEFAULT_DURATION_MINUTES = 5

# §4: шаг кнопки «+5 мин».
SNOOZE_STEP = timedelta(minutes=5)

# §3: пауза по умолчанию на неделю, если пользователь не выбрал дату окончания.
DEFAULT_PAUSE_DAYS = 7

# §7: пауза длиннее этого числа дней обнуляет текущую серию при возобновлении.
PAUSE_STREAK_RESET_DAYS = 14

# §7: окно расчёта процента выполнения.
STATS_WINDOW_DAYS = 30

# §6.1: горизонт генерации occurrences — сегодня плюс двое суток.
# Запас на случай, если ночной джоб не отработал.
GENERATION_HORIZON_DAYS = 2

# Дни недели 1-7 (пн-вс), как в §3.
WEEKDAYS = frozenset(range(1, 8))
