# Промпты для следующих сессий

Заготовки, чтобы не собирать контекст заново каждый раз. Состояние проекта — в
`HANDOFF.md`, он остаётся главным документом; здесь только точки входа в
конкретные задачи.

## Как запускать

Последовательность именно такая:

1. `/clear` — новая сессия в том же окне, рабочая папка и настройки сохранятся
2. `/model opus` или `/model sonnet` — **после** `/clear`, потому что он сбрасывает
   модель на Sonnet по умолчанию, и переключение действует ровно на одну сессию
3. вставить промпт

## Порядок

Сначала тестирование, потом починка найденного, потом документация.
Документировать непроверенный код — значит закрепить баги как спецификацию:
любая найденная позже ошибка потребует правки ещё и в диаграммах.

Тестирование прошло 26.08.2026 и дало пятнадцать находок. Решения по всем
приняты и записаны в спеку, поэтому сессии 2-5 это починка.

Сессии 1-3 закрыты. Находки 3, 4, 5, 8 и 9 починены, осталось шесть:
6, 7, 11, 12 — сессия 4, и 13, 15 — сессия 5. Следующий промпт — сессия 4.

Порядок внутри починки не произвольный:

- **Тесты идут первыми.** Всё, что правят сессии 3 и 4, сейчас не покрыто ни
  одним тестом: текущие 49 работают без БД, а эти функции все ходят в неё.
  Без сетки правки в трёх файлах сервисного слоя проверяются только руками,
  четвёртым подряд ручным прогоном.
- **Сессии 3 и 4 разбиты по функциям, а не по важности.** Находки 6 и 11
  правят одну и ту же `close_local_day`, находки 3 и 4 — одну
  `change_user_timezone`. Разложить их иначе значит дважды трогать один файл
  в разных сессиях и разбираться, чья правка что сломала.
- **Фронт последним:** он зависит от того, что к тому моменту отдаёт API.

Шаг 6 логично делать после починки: вдруг всплывёт что-то, что имеет смысл
починить в той же миграции `0003`. Заводить отдельную миграцию ради одной
колонки будет обидно.

---

## 1. Тестирование — Opus ✅ сделано 26.08.2026

**Сделано.** Пятнадцать находок, все разобраны, решения записаны в спеку —
см. «Находки ручного тестирования» и таблицу решений в HANDOFF.md. Промпт
оставлен как есть: он ещё пригодится, когда придёт время прогнать то же самое
по починенному коду.

Модель Opus, потому что работа тут не «прокликать», а придумать, что сломать.
Sonnet добросовестно пройдёт счастливые пути и скажет «всё работает».

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

План MVP пройден, шаг 5b закрыт и запушен. Хочу пройти приложение тестами —
не «всё ли открывается», а найти расхождения между тем, что делает код,
и тем, что написано в §4, §5, §7 и §9 спеки.

Работаем так: ты ведёшь план проверок, я кликаю в браузере и говорю, что вижу.
Ты параллельно смотришь в БД и логи воркера и бота — на экране видно не всё,
и половина интересного происходит именно там. Стек поднят: веб на
localhost:5173, API на :8000, вход кнопкой дев-логина.

Перед началом:
- поставь SCHEDULER_TICK_SECONDS=5 в .env и подними воркер заново через
  docker compose up -d worker (restart переменные не перечитывает), иначе
  один цикл занятия ждать час
- прогони сид, чтобы стартовать с известного состояния

Что интересует в первую очередь — места, где состояние живёт дольше одного
экрана и переживает перезагрузку:
- переходы §4 из каждого статуса, в том числе запрещённые
- лимит в 5 снузов и что происходит на шестом
- занятие, оставленное в in_progress, и что с ним сделает закрытие дня
- смена таймзоны, когда на сегодня уже есть занятия
- пауза длиннее 14 дней и обнуление серии, архив и восстановление
- расхождения между тем, что показывает веб, и тем, что лежит в БД

Две находки уже есть и описаны в HANDOFF, раздел «Находки ручного
тестирования»: подписка на бота и рассинхрон чата. Их не ищи, они известны.

Важно: сначала находим и записываем, потом чиним. Не бросайся править по ходу,
я хочу сперва увидеть весь список целиком. Каждую находку формулируй как
«что сделал → что ожидал по спеке → что получил», со ссылкой на пункт.
Если расхождение с документом, а не баг в коде — так и скажи, спека тоже
может быть неправа.

Итог сессии: раздел находок в HANDOFF.md обновлён, коммит.
```

Playwright MCP настроен 26.08.2026 и браузером управляет сам, так что строку
про «я кликаю и говорю, что вижу» из промпта можно убирать. Одна оговорка:
в конфиге ему принудительно задан `--browser chromium`. По умолчанию он просит
канал `chrome`, то есть настоящий Google Chrome, а его установка на этой машине
требует прав администратора. Если MCP вдруг снова начнёт жаловаться, что
браузер не найден, — смотреть надо туда.

---

## 2. Тесты на роутеры и сервисы с БД — Sonnet ✅ сделано 26.08.2026

Работа механическая: развилок нет, поведение уже описано в спеке. Opus здесь
не нужен.

Эта сессия идёт **перед** починкой находок, а не после. Всё, что предстоит
править сессиям 3 и 4, сейчас не покрыто ни одним тестом — проверять правки
будет нечем, кроме четвёртого подряд ручного прогона.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

Сессия тестирования нашла пятнадцать расхождений. Решения по всем приняты и
записаны в спеку, но кода они ещё не касались — чинить будут следующие две
сессии. Перед этим нужна сетка тестов, иначе правки нечем проверять.

Сейчас в backend/tests/ пять файлов, и все про чистые функции: переходы,
стрики, тексты, callback_data, timeutils. Ни одна функция, которую предстоит
править, не покрыта — потому что все они ходят в БД, а текущие 49 тестов
работают без неё. Непокрыты: change_user_timezone, cancel_pause, pause_habit,
archive_habit, restore_habit, close_local_day, generate_for_habit, regenerate.

Что нужно:

1. Фикстуры с настоящим Postgres — сессия на тест, откат транзакции после.
   Отдельная база в том же контейнере db, не та, на которой я кликаю: тесты
   не должны сносить мой сид.

2. Тесты на восемь перечисленных функций. Там, где текущее поведение
   правильное, фиксируй его как есть. Там, где текущее поведение — это
   находка из HANDOFF, пиши тест на РЕШЁННОЕ поведение из спеки и помечай
   xfail со ссылкой на номер находки. Не закрепляй баг зелёным тестом: список
   xfail станет чек-листом для сессий починки, они будут снимать пометки
   по одной.

3. Тесты на роутеры поверх этого: коды ответов, доступ к чужому ресурсу,
   превращение доменных ошибок в HTTP-коды.

Находки не чини. Задача этой сессии — сетка, а не правки. Если по ходу
захочется исправить что-то на месте — не надо, запиши и иди дальше.

Критерий: pytest зелёный, xfail'ы перечислены отдельно и их номера совпадают
с находками 3, 4, 6, 7, 8, 9, 11, 12. Проверю тем, что сломаю руками одну
из восьми функций и посмотрю, упадёт ли тест.
```

---

## 3. Починка: таймзона и паузы — Opus ✅ сделано 26.08.2026

Находки 3, 4, 5, 8 и 9. Решения по ним уже приняты, так что доменных развилок
в сессии нет — но правок в связанных местах много, и цена ошибки высокая:
именно здесь живёт 500, из-за которого профиль залипает.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

Чиним находки 3, 4, 5, 8 и 9 из раздела «Находки ручного тестирования».

Решения по ним закрыты — таблица «Решения по итогам сессии тестирования»
в HANDOFF и правки в docs/requirements.md. Спека источник истины: если код
разойдётся с ней, прав документ. Переобсуждать решения не надо.

У всех пяти одна причина: занятие оказывается в состоянии, которого расписание
не ждёт. Поэтому одной сессией.

Что предстоит:
- change_user_timezone пересчитывает paused вместе с pending (находка 4).
  Это же снимает 500 из находки 5
- ручка предпросмотра смены таймзоны: сколько сегодняшних занятий исчезнет,
  и предупреждение в «Настройках» до сохранения (находка 3)
- обработчик IntegrityError в app/api/main.py -> 409 (находка 5, вторая
  половина). Уникальный индекс — бизнес-правило, а не сбой инфраструктуры
- pause_habit не принимает starts_on раньше сегодняшнего дня по таймзоне
  пользователя (находка 8)
- ручка heatmap достраивает дни паузы из HabitPause (находка 9). Occurrences
  не трогаем: генератор по-прежнему пропускает дни паузы, и это правильно

Порядок выбери сам, но начни с находки 4 — она снимает 5 и делает стенд
пригодным для проверки остального.

Критерии, проверю все четыре:
- сценарий находки 4 не даёт двух занятий на один день
- смена таймзоны туда-обратно отдаёт 200
- пауза на 20 дней красится в heatmap целиком, все двадцать дней
- пауза задним числом отдаёт 400

Тесты на роутеры к этому моменту уже есть. Сними xfail с тестов по этим
находкам — они должны стать зелёными сами, без правки самих тестов. Если
тест приходится переписывать, чтобы он позеленел, — это повод остановиться
и спросить меня, а не подгонять.

Коммить по ходу: каждая находка отдельным коммитом.
```

---

## 4. Починка: жизненный цикл занятия — Opus

Находки 6, 7, 11 и 12. Две из них правят одну и ту же функцию, поэтому
разносить их по сессиям бессмысленно.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

Чиним находки 6, 7, 11 и 12. Решения закрыты — таблица «Решения по итогам
сессии тестирования» в HANDOFF и правки в спеке. Спека источник истины,
переобсуждать не надо.

Все четыре про одно: где кончается «занятие живо».

- close_local_day пропускает записи с current_due_at в будущем (находка 6).
  Снуз около полуночи переносит срок в новые сутки, а local_date остаётся
  вчерашним — джоб убивал занятие раньше обещанного кнопкой срока
- close_local_day закрывает in_progress в skipped, а не в missed (находка 11).
  Та же функция, что и выше: правь вместе, но коммить раздельно
- archive_habit закрывает открытые занятия текущего дня как skipped
  (находка 7). Сейчас архивация удаляет только будущие pending, и по
  заархивированной привычке продолжают приходить догоняющие пинги
- messages.reminder добавляет «Выполнил» в первое уведомление (находка 12),
  чтобы наборы кнопок в чате и в вебе совпали

Осторожно с close_local_day. Сейчас это один массовый UPDATE, а после правки
станет два разных перехода. Не утащи решение о статусе в SQL так, чтобы его
не стало видно в services/occurrences.py — инвариант 4 распространяется и на
массовые операции.

Критерии:
- занятие, отложенное снузом за полночь, доживает до своего срока
- брошенный in_progress за вчера закрывается в skipped, не в missed
- архивация гасит сегодняшнее занятие, пинги прекращаются
- наборы кнопок в вебе и в чате совпадают на статусе notified

Проверять буду вживую, поэтому поставь SCHEDULER_TICK_SECONDS=5 и подними
воркер через docker compose up -d worker — restart переменные не перечитает.

Сними xfail с тестов по этим находкам. Коммить по ходу, каждая находка
отдельным коммитом.
```

---

## 5. Починка: пауза в интерфейсе — Sonnet

Находки 13 и 15. Вёрстка и одна строка в выборке, доменных развилок нет.
Самая короткая сессия из всех.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

Мелкая сессия: находки 13 и 15, обе про то, как пауза выглядит в интерфейсе.
Решения приняты, см. таблицу решений в HANDOFF и §9 спеки.

- на «Моих привычках» привычка на паузе выглядит ровно как активная. Нужна
  метка «на паузе до <дата>» на карточке. Саму форму заморозки в список не
  тащи: она остаётся на экране привычки, рядом с текущей серией и
  предупреждением про 14 дней, иначе это предупреждение придётся дублировать
  в двух местах
- habits.active_pauses фильтрует только по cancelled_at и потому предлагает
  «снять досрочно» для паузы, закончившейся месяц назад. Фильтровать ещё и
  по дате окончания

Второй пункт — бэкенд, поэтому проверь заодно экран привычки: список пауз
берётся оттуда же.

Инвариант 4 в силе: решать, идёт пауза или нет, должен бэкенд. Не считай
даты в компоненте — попроси у API готовый признак.

Критерий: «Медитация» на свежем сиде видна в списке как «на паузе до
2 сентября», а историческая пауза «Зарядки» из фикстур в «снять досрочно»
больше не показывается.
```

---


## 6. README и ERD — Sonnet

Механический перенос того, что уже есть в коде, в читаемый вид. Opus на этом
жечь незачем.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

План MVP пройден. Нужна документация: в этой сессии README.md в корне и ERD.

README пишется для человека, который видит проект впервые: что это такое,
как поднять, из каких процессов состоит, куда смотреть при отладке.
Не для меня-через-месяц — это HANDOFF.md, и не для агента — это CLAUDE.md.
Не дублируй их содержимое, а ссылайся.

ERD — диаграмма Mermaid (erDiagram) в docs/. Источник истины — код в
backend/app/models/, не спека и не твоя память: поля, типы и связи бери
оттуда, я буду сверять построчно. Обязательно отрази ключевой инвариант:
Habit — это шаблон, Occurrence — конкретное выполнение в конкретный день,
и всё состояние выполнения живёт на нём. Из диаграммы это должно быть видно
без чтения текста.

Формат — Mermaid прямо в markdown, GitHub его рендерит. Никаких внешних
инструментов и картинок.

Скриншотов экранов не надо, они ещё поменяются.

Коммить по ходу, как договорено в CLAUDE.md. C4 и сиквенсы — отдельная
сессия, их не трогай.
```

---

## 7. C4 и сиквенсы — Opus

Вся работа тут в том, что **не** рисовать. Сиквенс на «нажали кнопку» может быть
на 6 шагов или на 25, и полезен только первый. Выбрать, какие четыре потока из
пятнадцати достойны диаграммы, — судейское решение, и ошибка в нём не видна,
пока диаграммой не попробуешь воспользоваться.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.
README и ERD уже есть — посмотри их и не дублируй.

Нужен docs/architecture.md: C4 и сиквенс-диаграммы, всё на Mermaid.

По C4: уровень контекста и уровень контейнеров. Ниже спускайся, только если
реально есть что показать. Шесть сервисов compose и три процесса из одного
образа — не тот масштаб, где компонентный уровень что-то проясняет. Считаешь
иначе — скажи почему, прежде чем рисовать.

По сиквенсам рисуй только то, где участников больше одного и порядок
неочевиден. Мой список: цикл уведомления (воркер → Notifier → Telegram →
бот → сервис → БД), нажатие кнопки в чате, создание привычки с немедленной
генерацией occurrences, закрытие дня. Обычный CRUD диаграммой не проясняется,
его не рисуй.

Главное, чего я жду от этой сессии — решений о том, что НЕ показывать.
Сиквенс на 25 шагов бесполезен. Если выбор между полнотой и читаемостью —
выбирай читаемость, а важные детали выноси текстом под диаграммой.

Отдельно отрази то, что я иначе забуду:
- почему планировщик отдельным процессом, а не внутри API
- почему диспетчер сначала отправляет и только потом ставит notified_at
- что бот и воркер — два разных процесса, оба говорят с Telegram,
  но в разные стороны

Источник истины — код, не спека. Где они разошлись, спрашивай меня,
а не выбирай сам.
```

---

## 8. Шаг 6: синхронизация чата — Opus

Находка 2 из ручного тестирования. Подробное описание и обоснование решений — в
`HANDOFF.md`, раздел «Что дальше», поэтому промпт короткий: пересказывать в нём
то, что уже записано, значит однажды получить две расходящиеся версии.

```
В рабочей папке лежат HANDOFF.md, CLAUDE.md и docs/requirements.md. Прочитай их.

Делаем шаг 6 — синхронизацию чата с действиями в вебе. Находка 2 из раздела
«Находки ручного тестирования», план и обоснование решений — в «Что дальше».

Двигайся по этому плану, но сначала разбери развилку, которая там оставлена
нерешённой: откуда брать исходный текст сообщения при редактировании —
хранить в БД или пересобирать через services/messages.py. Предложи вариант
с обоснованием, я утвержу, и только потом пиши код.

Критерий: начал упражнение кнопкой в Telegram, закончил в вебе — в течение
одного тика сообщение в чате гаснет и показывает итог. Оба сообщения,
и первое уведомление, и догоняющий пинг.
```
